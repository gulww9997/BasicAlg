#include <iostream>
#include <algorithm>
using namespace std;

int num[500];

bool compare(int a,int b) {
	return a > b;
}

int main() {
	int N;
	cin >> N;

	for (int i = 0; i < N; i++)
		cin >> num[i];

	sort(num, num + N, compare);

	for (int i = 0; i < N; i++)
		cout << num[i] << " ";
}