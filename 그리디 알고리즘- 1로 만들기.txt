#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int N, K, count = 0;
	cin >> N >> K;

	while (N != 1) {
		if (N % K != 0) {
			N--;
			count++;
		}
		else if (N % K == 0) {
			N /= K;
			count++;
		}
	}

	cout << count;
}